const insert = require('./channel-insert');
const observable = require('./channel-observable');
const fetch = require('./channel-fetch');
const join = require('./channel-join');

module.exports = {
  insert,
  observable,
  fetch,
  join,
};
